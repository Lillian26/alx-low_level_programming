#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - Determine if an number is positive, negative or Zero.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int n = 122;

	while (n >= 97)
	{
		putchar(n);
		n--;
	}
	return (0);
}
